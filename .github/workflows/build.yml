name: Build

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Skip release creation'
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.12"

jobs:
  prepare:
    name: Prepare
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      should_build: ${{ steps.check.outputs.should_build }}
      version: ${{ steps.check.outputs.version }}
      is_nightly: ${{ steps.check.outputs.is_nightly }}
      tag: ${{ steps.check.outputs.tag }}
    steps:
      - uses: actions/checkout@v6
        with:
          # Start from the triggering ref so a Career-only fork can determine
          # whether it has a legacy dev release line before checking it out.
          ref: ${{ github.ref }}
          fetch-depth: 0

      - name: Select scheduled release branch
        id: release_line
        run: python3 tools/select_nightly_branch.py

      - name: Check build conditions
        if: steps.release_line.outputs.available == 'true'
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=$(python3 -c "import tomllib; print(tomllib.load(open('pyproject.toml', 'rb'))['project']['version'])")

          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            # Tagged stable release
            echo "is_nightly=false" >> $GITHUB_OUTPUT
            echo "should_build=true" >> $GITHUB_OUTPUT
            echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
            echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          elif [[ "$GITHUB_EVENT_NAME" == "workflow_dispatch" ]]; then
            # Manual snapshot dispatches are intentional force-builds.
            echo "is_nightly=true" >> $GITHUB_OUTPUT
            echo "should_build=true" >> $GITHUB_OUTPUT
            echo "tag=nightly-$(date -u +%Y%m%d)" >> $GITHUB_OUTPUT
            echo "version=$VERSION" >> $GITHUB_OUTPUT
          else
            # Scheduled developer snapshot from dev - only when something
            # user-facing changed since the previous snapshot.
            echo "is_nightly=true" >> $GITHUB_OUTPUT
            echo "tag=nightly-$(date -u +%Y%m%d)" >> $GITHUB_OUTPUT
            echo "version=$VERSION" >> $GITHUB_OUTPUT

            LAST_TAG=$(git tag --list "nightly-*" --sort=-version:refname | head -1)

            if [ -z "$LAST_TAG" ]; then
              echo "should_build=true" >> $GITHUB_OUTPUT
            else
              gh release view "$LAST_TAG" --json body -q .body > previous-notes.md || true
              : > latest-stable-notes.md
              LATEST_STABLE_TAG=$(gh api "repos/${GITHUB_REPOSITORY}/releases" \
                --jq '[.[] | select(.draft == false and .prerelease == false)][0].tag_name // ""')
              if [ -n "$LATEST_STABLE_TAG" ]; then
                gh release view "$LATEST_STABLE_TAG" --json body -q .body > latest-stable-notes.md || true
              fi
              python3 tools/release_notes.py should-build-nightly \
                --previous-tag "$LAST_TAG" \
                --exclude-notes previous-notes.md \
                --latest-stable-tag "$LATEST_STABLE_TAG" \
                --exclude-stable-notes latest-stable-notes.md \
                >> "$GITHUB_OUTPUT"
            fi
          fi

  build:
    name: ${{ matrix.label }}
    needs: prepare
    if: needs.prepare.outputs.should_build == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            label: Windows
          # Pinned: macos-latest migrates to macOS 26 from 2026-06-15
          # (actions/runner-images#14167). v1.8.0 shipped from macOS 15;
          # revalidate the Nuitka app build and ad-hoc codesign on the new
          # image before unpinning.
          - os: macos-15
            label: macOS
          - os: ubuntu-latest
            label: Linux
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event_name == 'schedule' && 'dev' || github.ref }}
          fetch-depth: 0
          lfs: true

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: uv sync --group dev --group build

      - name: Run tests
        env:
          SDL_VIDEODRIVER: dummy
          SDL_AUDIODRIVER: dummy
          FREIGHT_FATE_NO_SPEECH: "1"
        run: uv run pytest -q -n auto

      - name: Check release dependencies
        run: uv run python tools/build_release.py --check-dependencies

      - name: Build
        env:
          SDL_VIDEODRIVER: dummy
          SDL_AUDIODRIVER: dummy
          FREIGHT_FATE_NO_SPEECH: "1"
        run: >
          uv run python tools/build_release.py
          --tag "${{ needs.prepare.outputs.is_nightly == 'true' && needs.prepare.outputs.tag || needs.prepare.outputs.version }}"

      - name: Package AppImage
        if: matrix.label == 'Linux'
        timeout-minutes: 15
        run: >
          uv run python tools/build_appimage.py
          --tag "${{ needs.prepare.outputs.is_nightly == 'true' && needs.prepare.outputs.tag || needs.prepare.outputs.version }}"

      - name: Smoke test AppImage on Fedora
        if: matrix.label == 'Linux'
        timeout-minutes: 15
        run: |
          cat > fedora-smoke.sh <<'EOF'
          set -e
          dnf install -y -q alsa-lib xorg-x11-server-Xvfb dejavu-sans-fonts >/dev/null
          cd /work
          appimage=$(find dist -maxdepth 1 -name 'FreightFate-*-linux-x86_64.AppImage' -print -quit)
          if [ -z "$appimage" ]; then
            echo "AppImage was not created."
            exit 1
          fi
          chmod +x "$appimage"
          export FREIGHT_FATE_NO_SPEECH=1
          export SDL_AUDIODRIVER=dummy
          set +e
          timeout 60s xvfb-run -a "$appimage" --appimage-extract-and-run --smoke > fedora-smoke.log 2>&1
          status=$?
          set -e
          cat fedora-smoke.log
          if grep -E "error while loading shared libraries|cannot open shared object file" fedora-smoke.log; then
            echo "AppImage is missing a shared library on Fedora."
            exit 1
          fi
          if grep -E "cannot register existing type|GLib-GObject-CRITICAL" fedora-smoke.log; then
            echo "AppImage hit the GLib/GIO duplicate type-registration crash on Fedora."
            exit 1
          fi
          if [ "$status" -ne 0 ] && [ "$status" -ne 124 ]; then
            echo "AppImage exited unexpectedly with status $status on Fedora."
            exit "$status"
          fi
          echo "Fedora smoke test passed."
          EOF
          docker run --rm -v "$PWD:/work" fedora:43 bash /work/fedora-smoke.sh

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.label }}
          path: |
            dist/FreightFate-*.zip
            dist/FreightFate-*.tar.gz
            dist/FreightFate-*.AppImage
          # A rename or bad glob must fail the build, not silently ship a
          # release without this platform's download.
          if-no-files-found: error

  release:
    name: Release
    needs: [prepare, build]
    if: always() && needs.prepare.outputs.should_build == 'true' && needs.build.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event_name == 'schedule' && 'dev' || github.ref }}
          fetch-depth: 0

      - uses: actions/download-artifact@v7
        with:
          path: assets
          merge-multiple: true

      - name: Verify every platform archive is present
        run: |
          missing=0
          for suffix in windows-portable.zip macos.zip linux-x64.tar.gz linux-x86_64.AppImage; do
            if ! ls assets/FreightFate-*-"$suffix" >/dev/null 2>&1; then
              echo "Release is missing the $suffix download" >&2
              missing=1
            fi
          done
          exit $missing

      - name: Prepare release notes and checksums
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          IS_NIGHTLY: ${{ needs.prepare.outputs.is_nightly }}
          TAG: ${{ needs.prepare.outputs.tag }}
        run: |
          (cd assets && sha256sum * > checksums.txt)

          if [ "$IS_NIGHTLY" = "true" ]; then
            LAST_TAG=$(git tag --list "nightly-*" --sort=-version:refname | grep -v "^$TAG$" | head -1)
            if [ -n "$LAST_TAG" ]; then
              gh release view "$LAST_TAG" --json body -q .body > previous-notes.md || true
              python3 tools/release_notes.py nightly \
                --previous-tag "$LAST_TAG" \
                --exclude-notes previous-notes.md \
                --output notes.md
            else
              python3 tools/release_notes.py nightly --output notes.md
            fi
          else
            python3 tools/release_notes.py stable \
              --version "$TAG" --output notes.md
          fi

      - name: Create release
        if: ${{ inputs.dry_run != true }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          IS_NIGHTLY: ${{ needs.prepare.outputs.is_nightly }}
          TAG: ${{ needs.prepare.outputs.tag }}
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          if [ "$IS_NIGHTLY" = "true" ]; then
            gh release delete "$TAG" -y --cleanup-tag 2>/dev/null || true
            gh release create "$TAG" assets/* --notes-file notes.md \
              --title "Developer snapshot $(date -u +%Y-%m-%d)" \
              --prerelease --target dev
          else
            if gh release view "$TAG" &>/dev/null; then
              gh release upload "$TAG" assets/* --clobber
              gh release edit "$TAG" --notes-file notes.md --title "Freight Fate v${VERSION}"
            else
              gh release create "$TAG" assets/* --notes-file notes.md \
                --title "Freight Fate v${VERSION}"
            fi
          fi

      - name: Send build notification
        if: ${{ inputs.dry_run != true }}
        env:
          ORINKS_BUILD_NOTIFICATION_TOKEN: ${{ secrets.ORINKS_BUILD_NOTIFICATION_TOKEN }}
          IS_NIGHTLY: ${{ needs.prepare.outputs.is_nightly }}
        run: |
          if [ -z "$ORINKS_BUILD_NOTIFICATION_TOKEN" ]; then
            echo "ORINKS_BUILD_NOTIFICATION_TOKEN is not configured; skipping notification."
            exit 0
          fi

          python - <<'PY'
          import json
          import os
          import urllib.request

          product = "Freight Fate"
          release_kind = "developer snapshot" if os.environ["IS_NIGHTLY"] == "true" else "stable release"
          payload = {
              "product": product,
              "title": f"{product} {release_kind} available",
              "body": f"A new {product} {release_kind} is ready to download.",
              "url": "/freight-fate/downloads",
          }
          request = urllib.request.Request(
              "https://www.orinks.net/api/notifications/send",
              data=json.dumps(payload).encode("utf-8"),
              headers={
                  "Authorization": f"Bearer {os.environ['ORINKS_BUILD_NOTIFICATION_TOKEN']}",
                  "Content-Type": "application/json",
              },
              method="POST",
          )
          try:
              with urllib.request.urlopen(request, timeout=30) as response:
                  print(response.read().decode("utf-8"))
          except Exception as exc:
              print(f"Build notification failed: {exc}")
          PY
